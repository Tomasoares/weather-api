spring:
  application:
    name: weather-api

server:
  port: 8080
  servlet:
    context-path: /weather-api

open-weather:
  api-key: value
  url: api.openweathermap.org/data/2.5/
  weather-uri: /weather
  units: metric


